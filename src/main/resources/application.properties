# Server port (controlled by env PORT if provided)
server.port=${PORT:8080}

# Oracle Database
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/ORCL
spring.datasource.username=RM556652
spring.datasource.password=120206
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Swagger UI path
springdoc.swagger-ui.path=/swagger-ui.html

# Cache
spring.cache.type=simple

# Disable SQL init
spring.sql.init.mode=never

# Flyway disabled (kept for reference config)
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
jwt.secret=H2Qf7wqz0L3c4xZ9rP6sM8tA1bC5vN7kY2pR4uT6wX8z
# Prefer a base64-encoded 256-bit secret for HS256. In production, set this via environment variable or secret manager.
jwt.secret-base64=ZmRzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZA==
# expiration in milliseconds (24h)
jwt.expiration-ms=86400000

# Hikari (Oracle)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariPoolFleetOracle

# DevTools restart	spring.devtools.restart.enabled=false

# Notes:
# - application.yml still contains defaults (including H2 profile). Properties here will override base settings when no profile is used.
# - Ensure RabbitMQ settings are reachable (default in application.yml).