# Server port (controlled by env PORT if provided)

# Oracle Database (considere usar variáveis de ambiente como no YAML)
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/ORCL
spring.datasource.username=RM556652
spring.datasource.password=120206
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Alinhar com application.yml para permitir ajuste automático do schema em dev
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Dialect pode ser omitido para Oracle 12+; manter por compatibilidade
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

springdoc.swagger-ui.path=/swagger-ui.html

# Alinhar cache com YAML (caffeine)
spring.cache.type=caffeine

# Desabilitar inicialização automática de dados
spring.sql.init.mode=never

# Flyway desabilitado conforme solicitado
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
jwt.secret=H2Qf7wqz0L3c4xZ9rP6sM8tA1bC5vN7kY2pR4uT6wX8z
# Prefer a base64-encoded 256-bit secret for HS256. In production, set this via environment variable or secret manager.
jwt.secret-base64=ZmRzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZGZzZA==
# expiration in milliseconds (24h)
jwt.expiration-ms=86400000

# Development helper: static token to speed up mobile/local tests (do NOT use in production)
# dev.static-token=your-dev-static-token-here
# dev.static-user-email=admin@fleetzone.com

# Pool de conexões otimizado para Oracle
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariPoolFleetOracle

# Desliga o restart do DevTools para não duplicar contextos/sessões
spring.devtools.restart.enabled=false

# CORS configurado no SecurityConfig.java

# Allow Render (or any other host that provides $PORT) to set the server port
# Use the PORT env var if provided, otherwise default to 8080 locally
server.port=${PORT:8080}